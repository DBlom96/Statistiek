\documentclass[tikz, beamer, preview]{standalone}
\input{../styles/tikz_config.tex}

\newcommand{\radius}{1.5}


\begin{document}
    \begin{standaloneframe}
        \begin{tikzpicture}[scale=1.5]
            % Sample space S
            \draw[thick] (-0.5, 0.25) rectangle (8.5,5.5);
            \node at (0.4, 4.7) {\huge$S$};

            \only<+>{
                \begin{scope}
                    \clip (3, 3) circle (\radius cm);
                    \draw[black, pattern=north west lines, pattern color=blue] (3, 3) circle (\radius cm);
                \end{scope} 
                
                % Draw circles A and B
                \foreach \label/\x/\y/\angle in {A/3/3/135, B/5/3/45}
                {
                    \draw[thick] (\x, \y) circle (\radius cm) node[left] {};
                    \node[font=\bfseries] (\label) at ($(\x,\y)+(\angle:1.2*\radius)$) {\huge$\label$};
                }

                \node (event) at (4, 0.75) {\Huge\bfseries $A$};
            }

            \only<+>{
                \begin{scope}
                    \clip (5, 3) circle (\radius cm);
                    \draw[black, pattern=north west lines, pattern color=blue] (5, 3) circle (\radius cm);
                \end{scope} 
                
                % Draw circles A and B
                \foreach \label/\x/\y/\angle in {A/3/3/135, B/5/3/45}
                {
                    \draw[thick] (\x, \y) circle (\radius cm) node[left] {};
                    \node[font=\bfseries] (\label) at ($(\x,\y)+(\angle:1.2*\radius)$) {\huge$\label$};
                }

                \node (event) at (4, 0.75) {\Huge\bfseries $B$};
            }

            \only<+>{
                \begin{scope}
                    \draw[black, pattern=north west lines, pattern color=blue] (3, 3) circle (\radius cm);
                    \draw[black, pattern=north west lines, pattern color=blue] (5, 3) circle (\radius cm);
                \end{scope} 
                
                % Draw circles A and B
                \foreach \label/\x/\y/\angle in {A/3/3/135, B/5/3/45}
                {
                    \draw[thick] (\x, \y) circle (\radius cm) node[left] {};
                    \node[font=\bfseries] (\label) at ($(\x,\y)+(\angle:1.2*\radius)$) {\huge$\label$};
                }

                \node (event) at (4, 0.75) {\Huge\bfseries $A \cup B$: vereniging van $A$ en $B$};
            }

            \only<+>{
                \begin{scope}
                    \clip (3, 3) circle (\radius cm);
                    \draw[black, pattern=north west lines, pattern color=blue] (5, 3) circle (\radius cm);
                \end{scope} 
                
                % Draw circles A and B
                \foreach \label/\x/\y/\angle in {A/3/3/135, B/5/3/45}
                {
                    \draw[thick] (\x, \y) circle (\radius cm) node[left] {};
                    \node[font=\bfseries] (\label) at ($(\x,\y)+(\angle:1.2*\radius)$) {\huge$\label$};
                }

                \node (event) at (4, 0.75) {\Huge\bfseries $A \cap B$: doorsnede van $A$ en $B$};
            }

            \only<+>{
                \begin{scope}
                    \draw[black, pattern=north west lines, pattern color=blue] (-0.5, 0.25) rectangle (8.5, 5.5);
                    \filldraw[draw=black, fill=white] (3, 3) circle (\radius cm);
                \end{scope} 
                
                % Draw circles A and B
                \foreach \label/\x/\y/\angle in {A/3/3/135, B/5/3/45}
                {
                    \draw[thick] (\x, \y) circle (\radius cm) node[left] {};
                    \node[font=\bfseries] (\label) at ($(\x,\y)+(\angle:1.2*\radius)$) {\huge$\label$};
                }

                \node (event) at (4, 0.75) {\Huge\bfseries $A'$: complement van $A$};
            }
        \end{tikzpicture}
    \end{standaloneframe}
\end{document}